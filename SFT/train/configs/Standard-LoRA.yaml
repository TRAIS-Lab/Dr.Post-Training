# Standard LoRA fine-tuning with AdamW (no data curation, no compression)
# Note: curation params (n_val, selection_frac, val_strategy, val_batch_size) are ignored for Standard
method: Standard
finetuning: LoRA
lora_r: 32
lora_alpha: 1
lora_dropout: 0.1
