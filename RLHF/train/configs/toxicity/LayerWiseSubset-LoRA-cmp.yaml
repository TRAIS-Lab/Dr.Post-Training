# LayerWiseSubset with the compressed scoring backend (dense Gaussian 64x64; compared against exact PIP).
method: LayerWiseSubset
finetuning: LoRA
scoring:
  method: compress
  compression: normal-64*64
