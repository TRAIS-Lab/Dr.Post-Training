# Compile products
*.pyc
*.class
*.crc
*.o

# IDE specific
.scala_dependencies
.worksheet
*.iml
.idea/
.vscode/

# other
nohup.out
*.log
*.lock
*.un~
*.idx
.ipynb_checkpoints/
.project
.settings/
*/.cache-main
*/.cache-tests
*/.classpath
*/.project
*/.settings/
*.so
*.so.*
*.dylib
__pycache__
*.egg-info
target
build
dist
*.npy
*.mmap
*.pt
*.slurm
*.arrow
*.png
*.pdf
*.tex
*.nfs*
*.txt
*.pickle
*.pkl
*.safetensors
*.parquet

**/logs
*/data/train
*/data/eval
*/out
**/outputs
**/output
**/wandb

# Cluster-specific config and any local copy/backup of it (never commit; each user has their own)
cluster_env.sh*
submit.sh*

# debug
*debug*

# all files in result folder
*/result/*
**/results/

# all files in checkpoints folder
**/checkpoints*/
*.json

/test/
/Figures/
# Allow archive/RLVR for reference
/archive/*
**/runs*/

# Local-only notebook scaffolding (regenerates result.ipynb)
SFT/_build_result_nb.py

# torch.profiler chrome traces from SFT/benchmark/profile_step.py (tens of MB each)
SFT/benchmark/results/profiles/

# Benchmark results (SFT/benchmark/results/) stay local like every other result directory (see **/results/ above)

# QA array manifests (generated by SFT/train/qa_matrix_manifest.py; arrays index into them) and Slurm logs
SFT/train/manifests/
logs/

# Per-run exports written by the table generators (--scan); regenerated locally
SFT/tables/data/

# Hillclimb hook files
/.claude/settings.local.json
/.claude/settings.local.json.malformed-*
