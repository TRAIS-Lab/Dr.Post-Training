# Subset: global curation across all layers, per-mini-batch during PPO
#
# subset_mode: one_pass (faster) or two_pass (original GREATS)
# scoring.method: ghost, ghost_greats, direct, compress
#
method: Subset
finetuning: LoRA
subset_mode: two_pass
scoring:
  method: ghost
